/** @jsx React.DOM */
var React = require('react');

//components
var PublicMapTitle = require('app/components/public-map-title');
var Map = require('app/components/map');
var ShareMap = require('app/components/share-map');

module.exports = React.createClass({
  render: function () {
    console.log('mapview rendering')
    return  <div>
              <PublicMapTitle title={this.props.title} />
              <div id="map-container" >
                <Map id="map" width={960} height={500} selector="#map" data={this.props.data} />
              </div>
            </div>;
  }

});
