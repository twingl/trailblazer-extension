/** @jsx React.DOM */
var React = require('react');

module.exports = React.createClass({displayName: 'exports',
  getInitialState: function () {
    return {
      value: "Untitled Trail"
    };
  }

  render: function () {
    var value = this.state.value;
    return React.DOM.input({type: "text", value: value});

  }
});